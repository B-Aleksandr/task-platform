spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://localhost:5432/ordersystem
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8081

keycloak:
  auth-server-url: http://localhost:8080
  realm: task-realm
  client-id: task-client
  client-secret: KG3mUTb3vrbkCOLqWkqHTLnIjblnzrhV

logging:
  level:
    org.springframework.web.reactive.function.client: DEBUG